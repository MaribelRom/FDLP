function Square(){
let sideSquare=Number(prompt("Insertar en cm la medida de cada lado del cuadrado"))
let aereaSquare= sideSquare*sideSquare
return aereaSquare
}
function Rectangle(){
    let baseRectangle=Number(prompt("Insertar en cm la medida de cada lado del cuadrado"))
    let heightRectangle=Number(prompt("Insertar en cm la medida de cada lado del cuadrado"))
    let aereaRectangle= baseRectangle*heightRectangle
    return aereaRectangle
    }