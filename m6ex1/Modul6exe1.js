let firstNumber = Number ("Escriu un numero")
let secondNumber = Number ("Escriu el segon numero")

function showCalculate([firstNumber,secondNumber],myfunction){
    let firstNumber = Number ("Escriu un numero")
    let secondNumber = Number ("Escriu el segon numero")
}
 
function sumCalculate()