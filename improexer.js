const userArray=[]

function validateParameters(){
    
}

function improList(array,operationFunction){
    const userInput =prompt("what would you like to input as the first parametre?","funcion, array, string....")
    //userArray.push()=prompt()
    if (userInput!=="array"){
     }
     else{console.log("The first parameter is not an array")}

    const secondUserParameter =prompt("What would you like to input as the second parameter?","array,function,string...")
    if(secondUserParameter !=="function"){
        console.log("The second parameter is not a function")
    }
}
improList()
